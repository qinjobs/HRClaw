# uv 离线安装包（Windows x64）

本目录用于存放 Windows 10 / 11 的 `uv` 离线安装资源：

- `uv-x86_64-pc-windows-msvc.zip`
- `uv-x86_64-pc-windows-msvc.zip.sha256`
- `uv-installer.ps1`（官方参考脚本，离线包主要使用 zip）

推荐使用项目内脚本安装：

```text
install\windows\install_uv.bat
```
